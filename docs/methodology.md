# Methodology Notes

The core study evaluates whether stronger audio-lyrics representation alignment corresponds to improved four-quadrant music-emotion prediction.

The required baseline objective combines multimodal emotion prediction, auxiliary unimodal prediction, and symmetric cross-modal contrastive alignment.

The proposed extension will evaluate affect-aware alignment and curvature-safe adaptive alignment as separate, ablated components after the required baselines are established.
